<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>willeponken</title>
    <link>https://willeponken.me/</link>
    <description>Recent content on willeponken</description>
    <generator>Hugo -- gohugo.io</generator>
    <lastBuildDate>Sat, 21 May 2016 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://willeponken.me/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Gentoo with DM-Crypt LUKS and EFI (Work In Progress)</title>
      <link>https://willeponken.me/posts/gentoo_with_dm-crypt_luks/</link>
      <pubDate>Sat, 21 May 2016 00:00:00 +0000</pubDate>
      
      <guid>https://willeponken.me/posts/gentoo_with_dm-crypt_luks/</guid>
      <description>

&lt;p&gt;This article serves as somekind of meta instruction for installing Gentoo with DM-Crypt LUKS.&lt;/p&gt;

&lt;p&gt;The guide is heavily based upon &lt;em&gt;Sakaki&amp;rsquo;s EFI Install Guide&lt;/em&gt;: &lt;a href=&#34;https://wiki.gentoo.org/wiki/Sakaki%27s_EFI_Install_Guide&#34;&gt;https://wiki.gentoo.org/wiki/Sakaki%27s_EFI_Install_Guide&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;If this is your first time installing Gentoo it&amp;rsquo;s probably a better idea to follow &lt;em&gt;Sakaki&amp;rsquo;s EFI Install Guide&lt;/em&gt;, or follow &lt;em&gt;Gentoo&amp;rsquo;s Handbook&lt;/em&gt;: https:/wiki.gentoo.org/wiki/Handbook:AMD64/Full/Installation&lt;/p&gt;

&lt;h2 id=&#34;burn-minimal-installation-cd-to-usb:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Burn Minimal Installation CD to USB&lt;/h2&gt;

&lt;p&gt;Download the Minimal Installation CD from: &lt;a href=&#34;https://www.gentoo.org/downloads/&#34;&gt;https://www.gentoo.org/downloads/&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Write the ISO to the USB drive using DD:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;dd if=/path/to/image.iso of=/dev/sdX bs=8192k
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;boot-and-connect-to-a-network:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Boot and connect to a network&lt;/h2&gt;

&lt;p&gt;Either connect a network cable, or connect to a WLAN using:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;iwconfig eth0 essid &amp;lt;ssid-of-network&amp;gt;

# set hex WEP key
iwconfig eth0 key &amp;lt;hex-key&amp;gt;
# or use ASCII WEP key
iwconfig eth0 key s:&amp;lt;ascii-key&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;prepare-usb-key-and-its-filesystem:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Prepare USB-key and its filesystem&lt;/h2&gt;

&lt;p&gt;Check which USB storage device to use:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lsblk
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;After you have found the USB storage device create a primary partition on it:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;parted -a optimal /dev/sdY
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Then inside parted issue these commands:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(parted) mklabel gpt
(parted) mkpart primary fat32 0% 100%
(parted) set 1 BOOT on
(parted) quit
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Format the USB storage device to &lt;code&gt;fat32&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkfs.vfat -F32 /dev/sdY1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Temporarily mount the partition:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkdir /tmp/efiboot
mount -t vfat /dev/sdY1 /tmp/efiboot
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;create-keyfile-for-luks:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Create Keyfile for LUKS&lt;/h2&gt;

&lt;pre&gt;&lt;code&gt;export GPG_TTY=$(tty)
dd if=/dev/urandom bs=8388607 count=1 | gpg --symmetric --cipher-algo AES256 --output /tmp/efiboot/&amp;lt;hostname&amp;gt;-luks-key.gpg
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;create-gpt-partition-on-main-storage:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Create GPT partition on main storage&lt;/h2&gt;

&lt;p&gt;Find the main storage drive using:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lsblk
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Run &lt;code&gt;parted&lt;/code&gt; to create the GPT partition:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;parted -a optimal /dev/sdX
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Inside parted issue:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(parted) unit s
(parted) print free
...
Number  Start       End         Size        File System   Name          Flags
#       80000000s   500000000s  300000000s  Free Space    ...           ...
...
(parted) mkpart primary 80000000s 500000000s
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure everything is correct:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lsblk
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Overwrite the partition with pseudo-random data:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;dd if=/dev/urandom of=/dev/sdXn bs=1M
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;format-main-parition-with-luks:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Format main parition with LUKS&lt;/h2&gt;

&lt;p&gt;Unlock USB keyfile and let &lt;code&gt;cryptsetup&lt;/code&gt; format it as a LUKS partition:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;gpg --decrypt /tmp/efiboot/&amp;lt;hostname&amp;gt;-luks-key.gpg | cryptsetup --cipher serpent-xts-plain65 --key-size 512 --hash sha512 --key-file - luksFormat /dev/sdXn
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Verify everything went well:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cryptsetup luksDump /dev/sdXn
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Backup the LUKS header to the USB storage device (incase the header is damaged and is needed for restoring data):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cryptsetup luksHeaderBackup /dev/sdXn --header-backup-file /tmp/efiboot/&amp;lt;hostname&amp;gt;-luks-header.img
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;setup-lvm-on-main-luks-partition:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Setup LVM on main LUKS partition&lt;/h2&gt;

&lt;p&gt;Open the LUKS volume:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;gpg --decrypt /tmp/efiboot/&amp;lt;hostname&amp;gt;-luks-key.gpg | cryptsetup --key-file - luksOpen /dev/sdXn storage
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create LVM physical volume (PV):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;pvcreate /dev/mapper/storage
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create LVM volume group (VG):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;vgcreate vg1 /dev/mapper/storage
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Check the size of RAM:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;grep MemTotal /proc/meminfo
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create LVM logical volume (LV) for swap:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lvcreate --size &amp;lt;size-of-ram+2&amp;gt;G --name swap vg1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create LVM logical volume (LV) for root:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lvcreate --size 40G --name rot vg1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create LVM logical volume (LV) for home:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;lvcreate --extents 95%FREE --name home vg1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure everything is setup correct:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;pvdisplay
vgdisplay
lvdisplay
ls /dev/mapper
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;format-and-mount-the-logical-volumes-lvs:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Format and mount the logical volumes (LVs):&lt;/h2&gt;

&lt;p&gt;Format swap:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkswap -L &amp;quot;swap&amp;quot; /dev/mapper/vg1-swap
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Format root:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkfs.ext4 -L &amp;quot;root&amp;quot; /dev/mapper/vg1-root
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Format home:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkfs.ext4 -m 0 -L &amp;quot;home&amp;quot; /dev/mapper/vg1-home
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Activate swap:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;swapon /dev/mapper/vg1-swap
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Mount root directory at pre-existing &lt;code&gt;/mnt/gentoo&lt;/code&gt; mountpoint:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mount -t ext4 /dev/mapper/vg1-root /mnt/gentoo
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create needed directories in root:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkdir /mnt/gentoo/{home,boot,boot/efi}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Mount home directory:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mount -t ext4 /dev/mapper/vg1-home /mnt/gentoo/home
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Unmount USB boot key&amp;rsquo;s EFI partition:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;unmount /dev/efiboot
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Take note of the PARTUUIDs for the USB storage partition and the main partition:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;blkid /dev/sdYn /dev/sdXn
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;check-date-and-time:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Check date and time&lt;/h2&gt;

&lt;p&gt;Make sure the time is correct:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;date
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;fetch-and-unpack-the-gentoo-stage-3-tarball:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Fetch and unpack the Gentoo Stage 3 Tarball:&lt;/h2&gt;

&lt;p&gt;Change to the root mountpoint:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cd /mnt/gentoo
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Download the latest Stage 3 files (replace &lt;code&gt;YYYYMMDD&lt;/code&gt; with the current release):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;wget -c http://distfiles.gentoo.org/releases/amd64/autobuilds/current-stage3-amd64/stage3-amd64-YYYYMMDD.tar.bz2
wget -c http://distfiles.gentoo.org/releases/amd64/autobuilds/current-stage3-amd64/stage3-amd64-YYYYMMDD.tar.bz2.CONTENTS
wget -c http://distfiles.gentoo.org/releases/amd64/autobuilds/current-stage3-amd64/stage3-amd64-YYYYMMDD.tar.bz2.DIGESTS.asc
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Retrieve the public key:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;gpg --keyserver subkeys.pgp.net --recv-keys 0x9E6438C817072058
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Verify the cryptographic signature:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;gpg --verify stage3-amd64-*.DIGESTS.asc
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Check the digests:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;awk &#39;/SHA512 HASH/{getline;print}&#39; stage3-amd64-*.DIGESTS.asc | sha512sum -- check
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Double check you&amp;rsquo;re in the &lt;code&gt;/mnt/gentoo&lt;/code&gt; directory, then issue:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;tar xvjpf stage3-amd64-*.tar.bz2
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Remove the Stage 3 files:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;rm stage3-amd64-*
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Return home:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cd
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;setup-portage:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Setup Portage&lt;/h2&gt;

&lt;p&gt;Edit &lt;code&gt;/mnt/gentoo/root/.bashrc&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;export NUMCPUS=$(nproc)
export NUMCPUSPLUSONE=$(( NUMCPUS + 1 ))
export MAKEOPTS=&amp;quot;-j${NUMCPUSPLUSONE} -l${NUMCPUS}&amp;quot;
export EMERGE_DEFAULT_OPTS=&amp;quot;--jobs=${NUMCPUSPLUSONE} --load-average=${NUMCPUS}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Copy the &lt;code&gt;.bash_profile&lt;/code&gt; file (make sure &lt;code&gt;VIDEO_CARDS&lt;/code&gt; and &lt;code&gt;INPUT_DEVICES&lt;/code&gt; are correct):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cp /mnt/gentoo/etc/skel/.bash_profile /mnt/gentoo/root/
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Edit the &lt;code&gt;/mnt/gentoo/etc/portage/make.conf&lt;/code&gt; file:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;# C and C++ compiler options for GCC.
CFLAGS=&amp;quot;-march=native -O2 -pipe&amp;quot;
CXXFLAGS=&amp;quot;${CFLAGS}&amp;quot;

# Note: MAKEOPTS and EMERGE_DEFAULT_OPTS are set in .bashrc

# Only free software, please.
ACCEPT_LICENSE=&amp;quot;-* @FREE CC-Sampling-Plus-1.0&amp;quot;

# WARNING: Changing your CHOST is not something that should be done lightly.
# Please consult http://www.gentoo.org/doc/en/change-chost.xml before changing.
CHOST=&amp;quot;x86_64-pc-linux-gnu&amp;quot;

# Use the &#39;stable&#39; branch.
ACCEPT_KEYWORDS=&amp;quot;amd64&amp;quot;

# Additional USE flags in addition to those specified by the current profile.
CPU_FLAGS_X86=&amp;quot;&amp;quot;
USE=&amp;quot;${CPU_FLAGS_X86}&amp;quot;

# Important Portage directories.
PORTDIR=&amp;quot;/usr/portage&amp;quot;
DISTDIR=&amp;quot;${PORTDIR}/distfiles&amp;quot;
PKGDIR=&amp;quot;${PORTDIR}/packages&amp;quot;

# Turn on logging - see http://gentoo-en.vfose.ru/wiki/Gentoo_maintenance.
PORTAGE_ELOG_CLASSES=&amp;quot;info warn error log qa&amp;quot;
PORTAGE_ELOG_SYSTEM=&amp;quot;save&amp;quot;
# Logs go to /var/log/portage/elog by default - view them with elogviewer.

# Settings for X11 (make sure these are correct for your hardware).
VIDEO_CARDS=&amp;quot;intel i965&amp;quot;
INPUT_DEVICES=&amp;quot;evdev synaptics&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;prepare-and-enter-chroot:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Prepare and enter &lt;code&gt;chroot&lt;/code&gt;&lt;/h2&gt;

&lt;p&gt;Select the correct mirror for your location:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mirrorselect -i -o &amp;gt;&amp;gt; /mnt/gentoo/etc/portage/make.conf
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Setup &lt;code&gt;/mnt/gentoo/etc/portage/repos.conf&lt;/code&gt; directory:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkdir -p /mnt/gentoo/etc/portage/repos.conf
cp /mnt/gentoo/usr/share/portage/config/repos.conf /mnt/gentoo/etc/portage/repos.conf/gentoo.conf
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Edit the &lt;code&gt;/mnt/gentoo/etc/portage/repos.conf/gentoo.conf&lt;/code&gt; file:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;[DEFAULT]
main-repo = gentoo

[gentoo]
location = /usr/portage
sync-type = rsync
auto-sync = yes
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Select mirror for &lt;code&gt;sync-uri&lt;/code&gt; (rsync server location):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mirrorselect -i -r -o | sed &#39;s/^SYNC=/sync-uri = /;s/&amp;quot;//g&#39; &amp;gt;&amp;gt; /mnt/gentoo/etc/portage/repos.conf/gentoo.conf
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Copy resolv.conf so DNS works in &lt;code&gt;chroot&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cp -L /etc/resolv.conf /mnt/gentoo/etc/
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If we&amp;rsquo;re using WLAN, also run:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cp /etc/wpa.conf /mnt/gentoo/etc/
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Mount &lt;code&gt;/proc&lt;/code&gt;, &lt;code&gt;/sys&lt;/code&gt; and &lt;code&gt;/dev&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mount -t proc none /mnt/gentoo/proc
mount --rbind /sys /mnt/gentoo/sys
mount --rbind /dev /mnt/gentoo/dev
mount --make-rslave /mnt/gentoo/sys
mount --make-rslave /mnt/gentoo/dev
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Enter &lt;code&gt;chroot&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;chroot /mnt/gentoo /bin/bash
source /etc/profile
export PS1=&amp;quot;(chroot) $PS1&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;install-the-portage-tree:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Install the Portage Tree&lt;/h2&gt;

&lt;p&gt;Install the latest Portage repository tree snapshot (it&amp;rsquo;ll create all directories it&amp;rsquo;s complaining about):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge-webrsync
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Update the Portage tree:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --sync
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure the Portage package is up-to-date:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --ask --verbose --oneshot portage
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Check which baseline profile to use:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) eselect profile list
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Select the correct profile (change &lt;code&gt;1&lt;/code&gt; to the correct one):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) eselect profile set 1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make a sanity check of the profile, &lt;code&gt;make.conf&lt;/code&gt;, enviroment etc.:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --info
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;setup-timezone-and-locale:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Setup timezone and locale&lt;/h2&gt;

&lt;p&gt;Check for your timezone:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) ls /usr/share/zoneinfo
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Set the timezone:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) echo &amp;quot;Europe/Stockholm&amp;quot; &amp;gt; /etc/timezone
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Reconfigure the &lt;code&gt;sys-libs/timezone-data&lt;/code&gt; package so it picks up the new timezone:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --config sys-libs/timezone-data
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Set the locale by uncommenting the correct ones in &lt;code&gt;/etc/locale.gen&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;en_US ISO-8859-1
en_US.UTF-8 UTF-8
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Generate the new locales:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) locale-gen
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Find the number for the locale:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) eselect locale list
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Set the locale to the &lt;code&gt;C&lt;/code&gt; locale (we&amp;rsquo;ll change to the actual later):&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) eselect locale set 1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Reload the enviroment:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) env-update &amp;amp;&amp;amp; source /etc/profile &amp;amp;&amp;amp; export PS1=&amp;quot;(chroot) $PS1&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Find the correct key map:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) ls /usr/share/keymaps/i386/qwerty
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Choose the correct one (strip out&lt;code&gt;.map.gz&lt;/code&gt;) and edit &lt;code&gt;/etc/conf.d/keymaps&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;keymap=&amp;quot;sv&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;fix-for-emerge-output-due-to-multi-job-support:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Fix for emerge output due to multi-job support&lt;/h2&gt;

&lt;p&gt;Install needed packages:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) mkdir -p -v /etc/portage/package.use
(chroot) touch /etc/portage/package.use/zzz_via_autounmask
(chroot) echo -e &amp;quot;# required by asciidoc (pulled in by git)\napp-text/texlive-core xetex&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.use/texlive-core
(chroot) emerge --ask --verbose dev-vcs/git
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Add the &lt;code&gt;sakaki-tools&lt;/code&gt; repository by editing &lt;code&gt;/etc/portage/repos.conf/sakaki-tools.conf&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;[sakaki-tools]

# Various utility ebuilds for Gentoo on EFI
# Maintainer: sakaki (sakaki@deciban.com)

location = /usr/local/portage/sakaki-tools
sync-type = git
sync-uri = https://github.com/sakaki-/sakaki-tools.git
priority = 50
auto-sync = yes
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Pull in the new overlay:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emaint sync --repo sakaki-tools
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Wildcard mask everything in &lt;code&gt;sakaki-tools&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) mkdir -p -v /etc/portage/package.mask
(chroot) echo &#39;*/*::sakaki-tools&#39; &amp;gt;&amp;gt; /etc/portage/package.mask/sakaki-tools-repo
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Unmask only the packages we need:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) mkdir -p -v /etc/portage/package.unmask
(chroot) touch /etc/portage/package.unmask/zzz_via_autounmask
(chroot) echo &amp;quot;app-portage/showem::sakaki-tools&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.unmask/showem
(chroot) echo &amp;quot;sys-kernel/buildkernel::sakaki-tools&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.unmask/buildkernel
(chroot) echo &amp;quot;app-portage/genup::sakaki-tools&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.unmask/genup
(chroot) echo &amp;quot;app-crypt/staticgpg::sakaki-tools&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.unmask/staticgpg
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Explicity allow the packages from &lt;code&gt;sakaki-tools&lt;/code&gt; because they&amp;rsquo;re marked as unstable:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) mkdir -p -v /etc/portage/package.accept_keywords
(chroot) touch /etc/portage/package.accept_keywords/zzz_via_autounmask
(chroot) echo &amp;quot;*/*::sakaki-tools ~amd64&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.accept_keywords/sakaki-tools-repo
(chroot) echo -e &amp;quot;# all versions of efitools currently marked as ~ in Gentoo tree\napp-crypt/efitools ~amd64&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.accept_keywords/efitools&amp;lt;Paste&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Update &lt;code&gt;@world&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --ask --verbose --deep --with-bdeps=y --newuse --update @world
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure everything is configured correctly:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) dispatch-conf
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;build-the-linux-kernel:bb49be16fd08f75f2aac8764b2e4441f&#34;&gt;Build the Linux kernel&lt;/h2&gt;

&lt;p&gt;Permit licenses needed to build kernel:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) mkdir -p -v /etc/portage/package.license
(chroot) touch /etc/portage/package.license/zzz_via_autounmask
(chroot) echo &amp;quot;sys-kernel/gentoo-sources freedist&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.license/gentoo-sources
(chroot) echo &amp;quot;sys-kernel/linux-firmware freedist&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.license/linux-firmware
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Fetch the kernel sources and firmware:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) emerge --ask --verbose sys-kernel/gentoo-sources
(chroot) emerge --ask --verbose sys-kernel/linux-firmware
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure &lt;code&gt;/usr/src/linux&lt;/code&gt; points to current kernel version:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) readlink -v /usr/src/linux
(chroot) eselect kernel list
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Emerge &lt;code&gt;buildkernel&lt;/code&gt; and additional packages:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) echo -e &amp;quot;# ensure we can generate a bootable kernel and initramfs\nsys-kernel/genkernel-next cryptsetup gpg plymouth&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.use/genkernel-next
(chroot) echo -e &amp;quot;# for a smooth transition to Gnome\nsys-boot/plymouth gdm&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.use/plymouth
(chroot) echo -e &amp;quot;# required by plymouth (kernel mode setting library)\nx11-libs/libdrm libkms&amp;quot; &amp;gt;&amp;gt; /etc/portage/package.use/libkms
(chroot) emerge --ask --verbose sys-kernel/buildkernel app-crypt/efitools
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Setup buildkernel:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) buildkernel --easy-setup
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Example configuration:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;... significant amounts of output suppressed in what follows ...
* buildkernel: Warning: This system wasn&#39;t booted under UEFI, cannot check boot entries
* Current configuration (from /etc/buildkernel.conf):
	EFI system partition UUID:  2498f874-ad8f-484e-8aba-81ac1c9665b6
	LUKS root partition UUID:   8111286a-d24e-4ba2-b6af-d0650fab4130
	GPG keyfile partition UUID: DEFAULT (=EFI system partition UUID)
	GPG keyfile (for LUKS):     luks-key.gpg                        
	EFI boot directory:         /EFI/Boot                           
	EFI boot file:              bootx64.efi                         
  Plymouth theme:             NONE (textual boot)                 
	Boot-time keymap:           us                         
	Init system:                systemd                             

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 1 then Enter
* Please choose which EFI system partition to use (or GO BACK):
	Num Partition UUID                       Path       USB Win Use
	--- ------------------------------------ ---------- --- --- ---
	1) 2498f874-ad8f-484e-8aba-81ac1c9665b6 /dev/sdb1   Y  ??? [*]
	2) f236e16c-ca97-4c36-b0d5-4196fa1e9930 /dev/sda2   N  ??? [ ]
	3) GO BACK
	Your choice: selected item is OK so press 3 then Enter
* Current configuration (from /etc/buildkernel.conf - MODIFIED):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 2 then Enter
* Please choose which LUKS partition contains the root LVM logical volume:
	Num Partition UUID                       Path       USB Use
	--- ------------------------------------ ---------- --- ---
	1) 8111286a-d24e-4ba2-b6af-d0650fab4130 /dev/sda5   N  [*]
	2) GO BACK
	Your choice: selected item is OK so press 2 then Enter
* Current configuration (from /etc/buildkernel.conf):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 3 then Enter
* Current LUKS key settings:
* Using a GPG-encrypted keyfile for LUKS:
*  KEYFILEPARTUUID unset: assuming GPG keyfile on EFI system partition
* Please choose your desired LUKS key setting (or GO BACK):
	1) Use GPG-encrypted keyfile on EFI system partition
	2) Use GPG-encrypted keyfile on specific USB partition...
	3) Use fallback passphrase (no keyfile)
	4) GO BACK
	Your choice: selected item is OK so press 4 then Enter
* Current configuration (from /etc/buildkernel.conf):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 4 then Enter
* Current EFI boot file setting:
* EFI boot file path: /EFI/Boot/bootx64.efi
*  (under EFI system partition mountpoint)
* Please choose your desired EFI boot file setting (or GO BACK):
	1) Use /EFI/Boot/bootx64.efi (recommended for initial USB install)
	2) Use /EFI/Microsoft/Boot/bootmgfw.efi (fallback for certain systems)
	3) Use /EFI/Boot/gentoo.efi (recommended for post-install use)
	4) GO BACK
	Your choice: selected item is OK so press 4 then Enter
* Current configuration (from /etc/buildkernel.conf):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 5 then Enter
* Current boot splash settings:
* Using textual boot (no Plymouth)
* Please choose your desired boot splash setting (or GO BACK):
	1) Use textual boot (no Plymouth)
	2) Use Plymouth graphical boot splash (&#39;fade-in&#39;)
	3) GO BACK
	Your choice: selected item is OK so press 3 then Enter
* Current configuration (from /etc/buildkernel.conf):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 6 then Enter
* Please choose your desired boot-time keymap (or GO BACK):
* Boot-time keymap is currently &#39;us&#39;
	1) azerty     9) cz      17) gr       25) mk        33) sg       41) us
	2) be        10) de      18) hu       26) nl        34) sk-y     42) wangbe
	3) bg        11) dk      19) il       27) no        35) sk-z     43) sf
	4) br-a      12) dvorak  20) is       28) pl        36) slovene  44) GO BACK
	5) br-l      13) es      21) it       29) pt        37) trf
	6) by        14) et      22) jp       30) ro        38) trq
	7) cf        15) fi      23) la       31) ru        39) ua
	8) croat     16) fr      24) lt       32) se        40) uk
	Your choice: press 32 then Enter
	NB - select the appropriate keymap for your system!
* Keymap selected to be &#39;se&#39;
* Current configuration (from /etc/buildkernel.conf - MODIFIED):
	EFI system partition UUID:  2498f874-ad8f-484e-8aba-81ac1c9665b6
	LUKS root partition UUID:   8111286a-d24e-4ba2-b6af-d0650fab4130
	GPG keyfile partition UUID: DEFAULT (=EFI system partition UUID)
	GPG keyfile (for LUKS):     luks-key.gpg                        
	EFI boot directory:         /EFI/Boot                           
	EFI boot file:              bootx64.efi                         
	Plymouth theme:             NONE (textual boot)                 
	Boot-time keymap:           se                                  
	Init system:                systemd                             

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 7 then Enter
* Current init system settings:
* Targeting systemd init
* Please choose your desired init system setting (or GO BACK):
	1) systemd (select if unsure)  3) GO BACK
	2) OpenRC
	Your choice: selected item is OK so press 3 then Enter 
	NB - users wanting to use OpenRC should press 2 then Enter here
* Current configuration (from /etc/buildkernel.conf):
	... as before ...

* Please choose an option:
	1) Set EFI system partition  6) Set boot-time keymap
	2) Set LUKS root partition   7) Set init system
	3) Set LUKS key options      8) Exit without saving
	4) Set EFI boot file path    9) Save and exit
	5) Set boot splash options
	Your choice: press 9 then Enter
* Configuration saved to /etc/buildkernel.conf.
* Be sure to run buildkernel, to rebuild the kernel with the new
* settings, before rebooting.
... significant amounts of output suppressed in the above ...
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Build the kernel using &lt;code&gt;buildkernel&lt;/code&gt;:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(chroot) buildkernel --ask --verbose
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;TBC&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>